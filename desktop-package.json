{
  "name": "studiosync-desktop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":        "vite",
    "build":      "vite build",
    "preview":    "vite preview",
    "tauri":      "tauri",
    "tauri:dev":  "tauri dev",
    "tauri:build":"tauri build",
    "tauri:mac":  "tauri build --target universal-apple-darwin",
    "tauri:win":  "tauri build --target x86_64-pc-windows-msvc"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0",
    "vite":            "^5.2.0"
  }
}
