<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Audio capture from DAW / virtual device -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Screen recording (ScreenCaptureKit) -->
    <key>com.apple.security.screen-recording</key>
    <true/>

    <!-- Accessibility for CGEventPost (mouse/keyboard injection) -->
    <key>com.apple.security.temporary-exception.mach-lookup.global-name</key>
    <array>
        <string>com.apple.coreservices.uiagent</string>
    </array>

    <!-- Network (WebRTC, signaling) -->
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- File access for saving recordings -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- HAL plugin install (user-space, no kext) -->
    <key>com.apple.security.files.user-selected.executable</key>
    <true/>
</dict>
</plist>
